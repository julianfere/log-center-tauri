import NavContainer, { NavButton } from "./styled";

const Navbar = () => {
  return (
    <NavContainer>
      <h1>-</h1>
      <NavButton>Add log file</NavButton>
    </NavContainer>
  );
};

export default Navbar;
